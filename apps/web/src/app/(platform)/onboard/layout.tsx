import OnboardLayout from "@/components/core/layouts/onboard-layout";

function RootOnboardLayout({ children }: { children: React.ReactNode }) {
    return <OnboardLayout>{children}</OnboardLayout>;
}

export default RootOnboardLayout;
